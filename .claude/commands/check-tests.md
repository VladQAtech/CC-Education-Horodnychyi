# Check Tests

Review the test suite in the `tests/` directory of this project.

1. List all test files found
2. For each file, report:
   - Number of test functions (functions starting with `test_`)
   - Whether the file appears complete or needs work
3. Summarize: which areas have good coverage and which need more tests
